import React from 'react';
import PropTypes from 'prop-types';
import "./style.css"

Header.propTypes = {
    
};

functionHeader(props) {
    return (
        <div>
            Header
        </div>
    );
}

export default Header;